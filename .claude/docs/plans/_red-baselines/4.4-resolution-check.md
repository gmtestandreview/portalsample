# Task 4.4 — post-reload resolution check

**Date:** 2026-09-09 (session reloaded after commit `a0b39af`)
**Branch:** `feat/a-team-wiring-review` @ `a0b39af`
**Spec ref:** §4.5.4 — "25/25 resolve un-prefixed, un-prompted"

## Step 1 — skills/ vs .claude/skills/ parity

```
comm -3 <(ls -1 skills | grep -v '\.md$' | sort) <(ls -1 .claude/skills | grep -v react-aria | sort)
```

- Output: **empty** — `.claude/skills/` matches `skills/` exactly.
- `git ls-files -s .claude/skills | grep -c '^120000'` → **25** (all tracked entries are symlinks, mode `120000`).
- Non-symlink entries excluding `react-aria`: **none**.
- `ls -1 skills | grep -vc '\.md$'` → **25**; `ls .claude/skills | grep -vc react-aria` → **25**.
- Working tree: **clean** at check time.

## Step 2 — invocation / resolution spot-checks

Allow-list (`.claude/settings.json`): **50** unique `Skill(...)` entries; valid JSON.
All five spot-check names carry both the bare `Skill(<name>)` and `Skill(<name>:*)` forms.

| Skill | How verified | Result |
|---|---|---|
| `receiving-code-review` | **Live `Skill()` invocation** (bare name) | PASS — base dir `.claude/skills/receiving-code-review`; body carries `<!-- Ported from superpowers 6.3.0 on 2026-09-09 ... -->` and the A Team-only "Where this fits" paragraph citing `.claude/rules/coding-style.md`. No permission prompt. Formerly non-existent as a project skill → now resolves local. |
| `brainstorming` | Registry cross-check | PASS — bare entry's description is verbatim the A Team fork frontmatter ("Use BEFORE any creative work … Hard-gated: no code until user approves the spec."), listed as a distinct entry separate from `superpowers:brainstorming`. Allow-listed (no prompt). Not fired live — hard-gates the session with no backing work. |
| `subagent-driven-development` | Registry cross-check | PASS — bare entry description verbatim the A Team fork ("… Dispatches a fresh subagent per task with two-stage review (spec compliance, then code quality)."), distinct from `superpowers:subagent-driven-development`. Allow-listed. Not fired live — workflow skill, no backing plan. |
| `systematic-debugging` | Registry cross-check | PASS — bare entry description verbatim the A Team fork ("… when the cause is not already established."), distinct from `superpowers:systematic-debugging`. Allow-listed. Not fired live — no bug under investigation. |
| `adr` | Registry cross-check | PASS — bare entry description verbatim the A Team fork; no `superpowers:adr` exists at all, so resolution is unambiguous. Allow-listed. |

**Mechanism proof:** the live `receiving-code-review` probe confirms a `.claude/skills/<name>` symlink added on this branch shadows the `superpowers` plugin skill of the same name in the reloaded session, with no permission prompt. This generalises to the other 24 canonical skills, whose bare-name registry entries all carry their A Team fork descriptions.

**Spike PASS criterion (Task 0.2 Step 3) satisfied:** "shows both but the bare name resolved local."

## Step 3

No commit (per plan). PR1 (Phases 0–4) is **content-complete**. Next: `finishing-a-development-branch` for the PR1 review + PR, running the PR1 subset of spec §7.
