# Green Fixture — Clean React 19

Use this fixture to prove Phase 1 passes when:

- `createRoot` is present
- no legacy React APIs remain
- refs use explicit nullable initializers
- render-time side effects are absent
