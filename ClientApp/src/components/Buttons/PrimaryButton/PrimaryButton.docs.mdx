import { Meta, Title, Subtitle, Primary, Controls, ArgTypes, Stories } from '@storybook/addon-docs/blocks';
import * as PrimaryButtonStories from './PrimaryButton.stories';

<Meta of={PrimaryButtonStories} />

<Title />

<Subtitle>A prominent call-to-action button following the NMI design system</Subtitle>

## Primary Button

The primary button is used for the main action on a page or dialog. It draws attention and encourages user interaction.

### Use Cases

- **Main form submissions** — "Submit", "Save", "Continue"
- **Primary actions** — "Create", "Download", "Publish"  
- **Call-to-action buttons** — Desktop and mobile CTAs

### Accessibility

- Full keyboard support (Tab, Enter, Space)
- Proper ARIA attributes for disabled state
- High contrast ratio (WCAG AA compliant)
- Screen reader announcements for disabled state

<Primary />

## Variations

<Stories />

## Props

<Controls />

## Static API Reference

<ArgTypes of={PrimaryButtonStories} />

## Code Examples

### Light Mode (Default)

```tsx
import PrimaryButton from '@/components/Buttons/PrimaryButton';

export function LightModeExample() {
  return (
    <PrimaryButton onClick={() => alert('Clicked!')}>
      Submit
    </PrimaryButton>
  );
}
```

### Dark Mode

```tsx
import PrimaryButton from '@/components/Buttons/PrimaryButton';

export function DarkModeExample() {
  return (
    <PrimaryButton mode="dark" onClick={() => alert('Clicked!')}>
      Save Changes
    </PrimaryButton>
  );
}
```

### Disabled State

```tsx
import PrimaryButton from '@/components/Buttons/PrimaryButton';

export function DisabledExample() {
  return (
    <PrimaryButton disabled>
      Processing...
    </PrimaryButton>
  );
}
```

## Props API

Use the generated Controls and ArgTypes above as the source of truth for
`PrimaryButton` props. Keep prop names, native button attributes, and mode
descriptions in TypeScript and JSDoc rather than duplicating a hand-maintained
MDX API table.

## Design System Integration

The Primary Button follows the NMI design system specifications:

- **Light Mode**: Default NMI primary action treatment from `btn-primary`
- **Dark Mode**: Alternate NMI primary treatment from `btn-primary-dark`
- **Disabled**: Native disabled state forwarded through React Aria button behaviour
- **Hover**: Theme-owned hover treatment from the button SCSS
- **Focus**: Visible focus ring for keyboard navigation

Colour values live in `ClientApp/src/styles/_variables.scss` and
`ClientApp/src/styles/_buttons.scss`; do not copy hex values into this page.

## Best Practices

✅ Use for primary/main actions only  
✅ Use clear, action-oriented labels  
✅ Ensure sufficient contrast ratio  
✅ Provide visual feedback on hover/focus  
✅ Test with keyboard navigation  

❌ Don't use multiple primary buttons in one section  
❌ Don't use for secondary or tertiary actions  
❌ Don't disable without explanation  
❌ Don't use overly long button text  

## Related Components

- [SecondaryButton](/docs/components-buttons-secondarybutton--documentation) — For secondary actions
- [Alert Component](/docs/components-alert--documentation) — For success/error messages
- [Design Tokens](/docs/documentation-style-guide--documentation) — Color and spacing system
